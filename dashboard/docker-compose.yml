version: "3.8"

services:
  dashboard:
    container_name: dashboard
    restart: unless-stopped
    build: .
    env_file:
      - .env
    ports:
      - "5000:5005"
    volumes:
      - ./apps:/app/apps
